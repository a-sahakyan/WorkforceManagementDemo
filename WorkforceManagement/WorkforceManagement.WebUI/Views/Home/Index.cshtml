@*@using WorkforceManagement.Domain.Entities;
    @model IEnumerable<Employee>
    @{
        ViewData["Title"] = "Index";
    }

    @foreach(var item in Model)
    {
        <p>@item.Email</p>
        <p>@item.Name</p>

    }*@
@using System.Security.Claims;

<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <title></title>
</head>
<body>
    <h1>Index Page</h1>
    @if (!User.Identity.IsAuthenticated)
    {
        <p>
            
            {
            <button type="submit" value="shh">fff</button>
            }


        </p>

    }
    else
    {
        var user = User.Identity as ClaimsIdentity;

        <p>
            welcome @user.Name, <a href="/Account/Logout">Logout</a>
        </p>

        <p>
            you can visit the authorize page, @Html.ActionLink("Login now", "Login", "Account")
            AuthPage
        </p>
    }
    kkkkkkkkkkkkkkkkkkkkkkkkkk
    <form action="/Account/Login" method="post">
        you are not login, please @Html.ActionLink("Login now", FormMethod.Post)
    </form>


</body>
</html>
